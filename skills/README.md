# Skills

Each Codex skill should live in its own directory with a `SKILL.md`.

Example:

```text
skills/my-skill/
  SKILL.md
  scripts/
  references/
```

Use short, focused skills that describe when they should be used and what workflow to follow.
